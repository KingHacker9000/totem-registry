{
  "name": "@totem/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "check": "node --check src/registry.js && node --check src/node-protocol.js && node --check src/node-agent.js && node --test",
    "test": "node --test"
  }
}
